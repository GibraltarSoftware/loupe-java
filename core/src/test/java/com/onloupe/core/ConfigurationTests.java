package com.onloupe.core;

public class ConfigurationTests {
	/*
	 * @Test public final void listener() { //get the configuration class
	 * ListenerConfiguration curConfiguration = (new
	 * AgentConfiguration()).getListener();
	 * 
	 * //store off the current configuration boolean initialEnableConsole =
	 * curConfiguration.getEnableConsole();
	 * 
	 * //now change each value and see that the change takes, but only changes the
	 * property it should. curConfiguration.setEnableConsole(!initialEnableConsole);
	 * assert curConfiguration.getEnableConsole() == !initialEnableConsole;
	 * //Assert.AreEqual(MonitorConfiguration.Listener.EnableConsoleRedirector,
	 * curConfiguration.EnableConsoleRedirector);
	 * 
	 * //now set it back. curConfiguration.setEnableConsole(initialEnableConsole);
	 * assert curConfiguration.getEnableConsole() == initialEnableConsole;
	 * //Assert.AreEqual(MonitorConfiguration.Listener.EnableConsoleRedirector,
	 * curConfiguration.EnableConsoleRedirector);
	 * 
	 * //now change each value and see that the change takes, but only changes the
	 * property it should. assert curConfiguration.getEnableConsole() ==
	 * initialEnableConsole;
	 * //Assert.AreEqual(MonitorConfiguration.Listener.CatchUnhandledExceptions,
	 * curConfiguration.CatchUnhandledExceptions);
	 * 
	 * //now set it back. assert curConfiguration.getEnableConsole() ==
	 * initialEnableConsole;
	 * //Assert.AreEqual(MonitorConfiguration.Listener.CatchUnhandledExceptions,
	 * curConfiguration.CatchUnhandledExceptions); }
	 */
}